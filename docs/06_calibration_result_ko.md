# 오라클 캘리브레이션 1차 결과 — Boltz-2 ipTM은 사용 불가

날짜: 2026-08-01 · 실행: Slurm job 3404(1 seed), 3406(5 seeds) · GPU: RTX A6000 (sv4ka-n3)
수용체: MMP9 촉매 도메인 337 aa (UniProt P14780, 107–443) · MSA 캐시 사용

## 결과

| 항목 | 1 seed | **5 seeds** |
|---|---|---|
| 양성 F3B (Kd ~20 nM) | 0.3703 | **0.3388** |
| 음성 평균 | 0.3654 | **0.3418** |
| AUROC | 0.60 (n=12) | 0.75 (n=4, 양자화 심함) |
| **분리 z-score** | 0.038 | **−0.12** |
| 판정 | FAIL | **FAIL** |

**알려진 20 nM 결합체가 무작위 서열보다 오히려 낮은 점수를 받았습니다.**

## 결정적 관찰 — 잡음이 신호를 33배 압도

시드만 바꿨을 때 **같은 서열**의 점수 변동:

| 서열 | 5회 측정 | 범위 |
|---|---|---|
| polyA | 0.3972 / 0.3374 / 0.3393 / 0.2177 / 0.3900 | **0.179** |
| random0 | … 0.3398 / 0.3978 / 0.2680 | **0.289** |

- 시드 간 표준편차 **σ ≈ 0.100**
- 양성–음성 차이 **0.003**
- **신호/잡음 = 0.03**

즉 이 측정값은 **결합에 관한 정보를 거의 담고 있지 않습니다.**

### 필요한 시드 수 (검정력 계산)

| 검출하려는 차이 | 후보당 시드 | 후보당 시간(77초/회) |
|---|---|---|
| 0.10 | 16 | 20분 |
| 0.05 | 63 | 81분 |
| 0.03 | 176 | 226분 |

설계 루프는 후보가 수백 개입니다. **계산적으로 불가능합니다.**

## 해석

이 결과는 문헌과 일치하며, 그것을 **정량화**합니다. ACS Synth. Biol. 2025(5c00196)는
AlphaFold3가 압타머 *구조*는 잘 예측하지만 *"screening cases에서 유망한 압타머를 식별하는
데 실패한다"*고 보고했습니다. 저희 데이터는 그 **원인**을 수치로 보여줍니다 —
판별 신호(0.003)가 모델 자체의 실행 간 분산(σ=0.10)에 완전히 묻힙니다.

**이 발견은 양성 대조군의 타당성과 무관하게 성립합니다.** F3B가 잘못 모델링됐거나
촉매 도메인에 붙지 않더라도, **시드 간 분산은 모델의 성질**이지 리간드의 성질이 아닙니다.

## 게이트의 가치

캘리브레이션 게이트가 없었다면 8라운드 × 96후보로 **잡음을 최적화**하고, 그럴듯한
랭킹을 만들어 압타머를 합성했을 것입니다. FAIL 판정이 이를 막았습니다.
**음성 결과를 정직하게 보고하는 장치가 실제로 작동했습니다.**

## 아직 미확인 (정직한 한계)

1. **F3B의 결합 도메인 미상** — 촉매 도메인이 아닐 수 있음(PEX 또는 전장 필요)
2. **2′-F 변형 미반영** — 비변형 RNA로 접음
3. **다른 판독값의 가능성** — 아래 참조

## 다음 검증 (권장, 저비용)

**가설**: 점수(스칼라 요약)는 흔들려도 **예측된 결합 부위**는 안정적일 수 있다.

**검증**: F3B + 셔플 + polyA를 각 5시드로 예측하고, 시드 간 **접촉 잔기 집합의
Jaccard 일치도**를 비교한다. F3B만 일관된 패치에 붙고 대조군은 흩어진다면,
ipTM 대신 **포즈 재현성**을 오라클 신호로 쓸 수 있다.

비용: 15회 예측, 약 20분. 구현은 `src/pipeline/consensus.py`에 이미 존재.

**이것도 실패하면** 접근을 바꿔야 한다:
- 수용체 재정의(전장/PEX 포함) 후 재시도
- DNA 양성 대조군 확보(LVMH 특허 G-quadruplex 서열) — 2′-F 교란 제거
- 오라클을 물리 기반(MM/GBSA)이나 결정론적 도킹으로 교체
- **논지 재구성**: "공동접힘 신뢰도는 압타머 선별기로 부적합하다"는 정량적 음성 결과를
  AiDTA 대리보상 퇴화 발견과 묶어 방법론 비판 논문으로

## 원자료

`results/calibration.json` (job 3406) · 로그 `logs/mmp9apt-3404.out`, `logs/mmp9apt-3406.out`
